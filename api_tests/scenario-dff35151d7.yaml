# ********RoostGPT********
# Test generated by RoostGPT for test elasticLogStash using AI Type Open AI and AI Model gpt-4



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "scenario-dff35151d7.csv"
    fields:
      - "accounts_body"
      - "email"
    skipHeader: true

scenarios:
  - name: Create my account
    flow:
      - post:
          url: "/accounts"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
              hasProperty: "email"
          log: "Create my account with the specified information"
      - think: 1
      - get:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
              hasProperty: 
                - "id"
                - "did"
                - "name"
                - "image"
                - "email"
                - "settings"
                - "createdAt"
                - "updatedAt"
              headerEquals:
                - "api-version": "1.0.0"
          log: "Verify the account created"
      - think: 1
