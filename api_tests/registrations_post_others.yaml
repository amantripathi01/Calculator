# ********RoostGPT********
# Test generated by RoostGPT for test elasticLogStash using AI Type Open AI and AI Model gpt-4

# Test generated for /recipients/branches/{id}/registrations_post for http method type POST in artillery framework
# RoostTestHash=16a6858a84



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "registrations_post_others.csv"
    fields:
      - "id"
      - "reference"
      - "startDate"
      - "endDate"
      - "credentialUrl"
  plugins:
    expect: {}
    
scenarios:
  - name: "Non-Success API Responses"
    flow:
      - post:
          # Invalid UUID in path
          url: "/recipients/branches/invalid_uuid/registrations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 400
        think: 1
        
      - post:
          # Missing required fields in payload
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            reference: "{{ reference }}"
            endDate: "{{ endDate }}"
          expect:
            - statusCode: 400
        think: 1
        
      - post:
          # Invalid date format in payload
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            reference: "{{ reference }}"
            startDate: "invalid_date"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 400
        think: 1
        
      - post:
          # Invalid URL format in payload
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "invalid_url"
          expect:
            - statusCode: 400
        think: 1
        
      - post:
          # Unauthorized request
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            Token: "invalid_token"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 401
        think: 1
