# ********RoostGPT********
# Test generated by RoostGPT for test elasticLogStash using AI Type Open AI and AI Model gpt-4

# Test generated for /recipients/persons/{id}_get for http method type GET in artillery framework
# RoostTestHash=131d25110f



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "persons_id_get_success.csv"
    fields:
      - "id"
    skipHeader: true
  processor: "./my-functions.js"
  
scenarios:
  - name: "Test API responses for /recipients/persons/{id} endpoint"
    flow:
      - get:
          url: "/recipients/persons/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: { "api-version": "1.0.0" }
            - hasProperty: "id"
            - hasProperty: "initials"
            - hasProperty: "firstName"
            - hasProperty: "middleName"
            - hasProperty: "lastName"
            - hasProperty: "birthDate"
            - hasProperty: "birthPlace"
            - hasProperty: "phone"
            - hasProperty: "email"
            - hasProperty: "postalCode"
            - hasProperty: "houseNumber"
            - hasProperty: "houseNumberPostfix"
            - hasProperty: "countryCode"
            - hasProperty: "reference"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
          capture:
            - json: "$.id"
              as: "id"
            - json: "$.initials"
              as: "initials"
            - json: "$.firstName"
              as: "firstName"
            - json: "$.middleName"
              as: "middleName"
            - json: "$.lastName"
              as: "lastName"
            - json: "$.birthDate"
              as: "birthDate"
            - json: "$.birthPlace"
              as: "birthPlace"
            - json: "$.phone"
              as: "phone"
            - json: "$.email"
              as: "email"
            - json: "$.postalCode"
              as: "postalCode"
            - json: "$.houseNumber"
              as: "houseNumber"
            - json: "$.houseNumberPostfix"
              as: "houseNumberPostfix"
            - json: "$.countryCode"
              as: "countryCode"
            - json: "$.reference"
              as: "reference"
            - json: "$.createdAt"
              as: "createdAt"
            - json: "$.updatedAt"
              as: "updatedAt"
      - think: 1
