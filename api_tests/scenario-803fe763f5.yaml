# ********RoostGPT********
# Test generated by RoostGPT for test elasticLogStash using AI Type Open AI and AI Model gpt-4



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "scenario-803fe763f5.csv"
    fields:
      - "accountName"
      - "email"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Grant authorization"
    flow:
      - post: # Create an account
          url: "/accounts"
          json:
            name: "{{ accountName }}"
            email: "{{ email }}"
          headers:
            commit-hash: "928d28d"
          expect:
            - statusCode: 201
            - hasProperty: "accountId"
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "1.0.0"]
            - hasHeader: "commit-hash"
            - headerEquals: ["commit-hash", "928d28d"]
          capture:
            json: "$.accountId"
            as: "accountId"
      - think: 1
      - post: # Grant access
          url: "/authorizations"
          json:
            email: "{{ email }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d"
          expect:
            - statusCode: 201
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "email"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "1.0.0"]
            - hasHeader: "location"
            - matchesRegexp: ["location", "^/authorizations/.*"]
            - hasHeader: "commit-hash"
            - headerEquals: ["commit-hash", "928d28d"]
      - think: 1
      - get: # Confirm access
          url: "/authorizations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "authorizations"
            - hasProperty: "authorizations.0.id"
            - hasProperty: "authorizations.0.name"
            - hasProperty: "authorizations.0.email"
            - hasProperty: "authorizations.0.createdAt"
            - hasProperty: "authorizations.0.updatedAt"
            - equals: ["$.authorizations[0].id", "{{ accountId }}"]
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "1.0.0"]
            - hasHeader: "commit-hash"
            - headerEquals: ["commit-hash", "928d28d"]
