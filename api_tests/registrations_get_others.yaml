# ********RoostGPT********
# Test generated by RoostGPT for test elasticLogStash using AI Type Open AI and AI Model gpt-4

# Test generated for /recipients/persons/{id}/registrations_get for http method type GET in artillery framework
# RoostTestHash=318c363c90



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_others.csv"
    fields:
      - "id"
      - "credential-url"
      - "size"
      - "offset"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        pool: 10
scenarios:
  - name: "Test non-success API responses"
    flow:
      # Testing with an invalid id
      - get:
          url: "/recipients/persons/{{ id }}/registrations?credential-url={{ credential-url }}&size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
          think: 1

      # Testing with an invalid credential-url
      - get:
          url: "/recipients/persons/{{ id }}/registrations?credential-url=invalid&size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
          think: 1

      # Testing with an invalid size
      - get:
          url: "/recipients/persons/{{ id }}/registrations?credential-url={{ credential-url }}&size=invalid&offset={{ offset }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
          think: 1

      # Testing with an invalid offset
      - get:
          url: "/recipients/persons/{{ id }}/registrations?credential-url={{ credential-url }}&size={{ size }}&offset=invalid"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
          think: 1

      # Testing with an invalid URL
      - get:
          url: "/invalid/url"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 1
