# ********RoostGPT********
# Test generated by RoostGPT for test elasticLogStash using AI Type Open AI and AI Model gpt-4

# Test generated for /recipients/branches/{id}/registrations_get for http method type GET in artillery framework
# RoostTestHash=49b35fa9c1



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "registrations_get_others.csv"
    fields:
      - "id"
      - "credential-url"
      - "size"
      - "offset"
    skipHeader: true
  environments:
    dev:
      target: "http://dev.example.com"
    prod:
      target: "http://prod.example.com"

scenarios:
  - name: "Test non-successful API requests"
    flow:
      # Test with invalid UUID in URL
      - get:
          url: "/recipients/branches/invalid-uuid/registrations"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404

      - think: 5

      # Test with missing required query parameter
      - get:
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400

      - think: 5

      # Test with invalid query parameter value
      - get:
          url: "/recipients/branches/{{ id }}/registrations?size=invalid-size-value"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400

      - think: 5

      # Test with incorrect authorization token
      - get:
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            Token: "invalid-token"
          expect:
            - statusCode: 401

      - think: 5

      # Test with correct request but expect non-successful response
      - get:
          url: "/recipients/branches/{{ id }}/registrations?credential-url={{ credential-url }}&size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 500

      - think: 5
