# ********RoostGPT********
# Test generated by RoostGPT for test elasticLogStash using AI Type Open AI and AI Model gpt-4

# Test generated for /recipients/branches/{id}/registrations_get for http method type GET in artillery framework
# RoostTestHash=49b35fa9c1



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_others.csv"
    fields:
      - "id"
      - "credential-url"
      - "size"
      - "offset"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        pool: 10
scenarios:
  - name: "Test non-success API responses"
    flow:
      # Testing with an invalid UUID
      - get:
          url: "/recipients/branches/invalid_uuid/registrations?credential-url={{ credential-url }}&size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
          think: 1
      # Testing with an invalid token
      - get:
          url: "/recipients/branches/{{ id }}/registrations?credential-url={{ credential-url }}&size={{ size }}&offset={{ offset }}"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 401
          think: 1
      # Testing with an invalid URL
      - get:
          url: "/invalid_url"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 1
      # Testing with invalid query parameters
      - get:
          url: "/recipients/branches/{{ id }}/registrations?invalid_param=invalid"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
          think: 1
