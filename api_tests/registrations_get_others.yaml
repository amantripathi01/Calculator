# ********RoostGPT********
# Test generated by RoostGPT for test elasticLogStash using AI Type Open AI and AI Model gpt-4

# Test generated for /recipients/branches/{id}/registrations_get for http method type GET in artillery framework
# RoostTestHash=49b35fa9c1



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "registrations_get_others.csv"
    fields: 
      - "id"
      - "credentialUrl"
      - "size"
      - "offset"
    skipHeader: true
  plugins:
    expect: {}
  templates:
    headers:
      Token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-success API requests"
    flow:
      # Invalid ID in URL
      - get:
          url: "/recipients/branches/invalidID/registrations"
          headers: "{{ headers }}"
          expect:
            - statusCode: 404
          think: 1

      # Missing required ID in URL
      - get:
          url: "/recipients/branches//registrations"
          headers: "{{ headers }}"
          expect:
            - statusCode: 400
          think: 1

      # Invalid size query param
      - get:
          url: "/recipients/branches/{{ id }}/registrations?size=invalidSize"
          headers: "{{ headers }}"
          expect:
            - statusCode: 400
          think: 1

      # Invalid offset query param
      - get:
          url: "/recipients/branches/{{ id }}/registrations?offset=invalidOffset"
          headers: "{{ headers }}"
          expect:
            - statusCode: 400
          think: 1

      # Invalid authorization token
      - get:
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            Token: "invalidToken"
          expect:
            - statusCode: 401
          think: 1

      # Missing authorization token
      - get:
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            Token: ""
          expect:
            - statusCode: 401
          think: 1
