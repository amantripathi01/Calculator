# ********RoostGPT********
# Test generated by RoostGPT for test elasticLogStash using AI Type Open AI and AI Model gpt-4

# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=cb128291e8



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "./authorizations_id_delete_others.csv"
    skipHeader: true
    fields:
      - id
scenarios:
  - name: "Non-success API requests"
    flow:
      - comment: "Attempt to DELETE with invalid authorization token"
        delete:
          url: "/authorizations/{{ id }}"
          headers:
            Token: "InvalidToken"
        expect:
          - statusCode: 401
        think: 1

      - comment: "Attempt to DELETE with missing authorization token"
        delete:
          url: "/authorizations/{{ id }}"
        expect:
          - statusCode: 401
        think: 1

      - comment: "Attempt to DELETE with valid token but non-existing id"
        delete:
          url: "/authorizations/non-existing-id"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        think: 1
