# ********RoostGPT********
# Test generated by RoostGPT for test elasticLogStash using AI Type Open AI and AI Model gpt-4



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "scenario-87771cdd6d.csv"
    fields:
      - "authorizationId"
    skipHeader: true
  environments:
    dev:
      target: "http://localhost:8080"
      variables:
        AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Delete authorization"
    flow:
      - log: "Deleting authorization with Authorization ID: {{ authorizationId }}"
      - delete:
          url: "/authorizations/{{ authorizationId }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals:
                - "api-version"
                - "1.0.0"
      - think: 1
      - log: "Checking if authorization was successfully deleted"
      - get:
          url: "/authorizations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "authorizations"
            - notHasProperty: "authorizations.{{ authorizationId }}"
            - hasHeader: "api-version"
            - headerEquals:
                - "api-version"
                - "1.0.0"
