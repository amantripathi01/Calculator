# ********RoostGPT********
# Test generated by RoostGPT for test elasticLogStash using AI Type Open AI and AI Model gpt-4

# Test generated for /recipients/branches/{id}/registrations_get for http method type GET in artillery framework
# RoostTestHash=49b35fa9c1



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_success.csv"
    fields:
      - "id"
    skipHeader: true
  defaults:
    headers:
      Token: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Test for non-success API responses"
    flow:
      # Get all registrations for a specific recipient branch
      - get:
          url: "/recipients/branches/{{ id }}/registrations?credential-url={{ credentialUrl }}&size={{ size }}&offset={{ offset }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "registrations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
      - think: 1
      # Get a specific registration for a specific recipient branch
      - get:
          url: "/recipients/branches/{{ id }}/registrations/{{ registrationId }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "reference"
            - hasProperty: "startDate"
            - hasProperty: "endDate"
            - hasProperty: "status"
            - hasProperty: "credentialUrl"
            - hasProperty: "suspendedUntil"
            - hasProperty: "issuedAt"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
      - think: 1
